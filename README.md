# Gênny & Sophie Studio
Native Android photo-studio project for Galaxy A55 5G.

Implemented foundation: character selector, text prompt, Android voice recognition, reference-image picker, local history, local-model manager boundary, and GitHub Actions debug APK build.

The project does not claim image generation until a compatible on-device inference runtime and model weights are actually integrated and tested. No paid API is wired into the app.
